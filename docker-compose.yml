services:
  cam:
    build:
      context: ./cam
    container_name: cam-listener
    network_mode: host

  lamppost:
    build:
      context: ./lamppost
    container_name: lamppost-listener
    network_mode: host

  semaphore:
    build:
      context: ./semaphore
    container_name: semaphore-listener
    network_mode: host
